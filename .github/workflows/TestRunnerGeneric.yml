name: TestRunnerGeneric

on:
  workflow_call:
    inputs:
      test-names:
        required: true
        type: string

jobs:
  test-runner-cortexm:
    runs-on: ubuntu-22.04
    container:
      image: ghcr.io/victor-jung/deeploy:main
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Build Deeploy
        run: pip install -e .
      - name: Run Test
        run: |
          # Convert the inputs into arrays
          testNames="${{ inputs.test-names }}"
          IFS=$'\n' read -r -d '' -a testName_array <<< "$testNames"
          cd DeeployTest
          for testName in "${testName_array[@]}"; do
            python testRunner_generic.py -t Tests/$testName
          